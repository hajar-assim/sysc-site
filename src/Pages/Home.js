import NavBar from "../NavBar";

const Home = () => {
    return (
        <>
        <NavBar/>
        <p>Select a Unit of Measurement to start converting!!</p>
        </>
    );
}

export default Home